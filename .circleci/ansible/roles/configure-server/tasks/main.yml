---
- name: "upgrade packages."
  become: true
  apt:
    upgrade: "yes"

- name: remove dependencies that are no longer required
  become: yes
  apt:
    autoremove: yes    

- name: "install dependencies."
  become: true
  apt:
    name: ["nodejs", "npm"]
    state: latest
    update_cache: yes

- name: "install pm2"
  become: true
  npm:
    name: pm2
    global: yes
    production: yes
    state: present   
 
- name: Executing node
  become: true
  shell: |
    pm2 start  npm -- run start:prod
  environment:
      ENVIRONMENT: production
      AWS_ACCESS_KEY_ID: AKIA5F6LQW2IPA7YUDPO
      AWS_SECRET_ACCESS_KEY: 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
      AWS_DEFAULT_REGION: us-east-2
      TYPEORM_CONNECTION: postgres
      TYPEORM_MIGRATIONS_DIR: ./src/migrations
      TYPEORM_MIGRATIONS: ./src/migrations/*.ts
      TYPEORM_CONNECTION: postgres
      TYPEORM_ENTITIES: ./src/modules/domain/**/*.entity.ts
      TYPEORM_HOST: database-1.cuxnzhvbpvrv.us-east-2.rds.amazonaws.com
      TYPEORM_PORT: 5432
      TYPEORM_USERNAME: masterUsername 
      TYPEORM_PASSWORD: 123456789
      TYPEORM_DATABASE: database-1